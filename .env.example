# =====================================================
# Configuración de Base de Datos
# =====================================================
# Para producción: usar contraseñas fuertes (mínimo 32 caracteres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=integra_rls
DB_USER=postgres
DB_PASSWORD=CAMBIAR_POR_CONTRASEÑA_SEGURA_MIN_32_CHARS

# Pool de Conexiones
DB_POOL_MIN=2
DB_POOL_MAX=20

# Sesión (timeout en minutos)
SESSION_TIMEOUT=60

# =====================================================
# Seguridad
# =====================================================
# Clave secreta para firmar sesiones (generar con: openssl rand -hex 32)
SECRET_KEY=GENERAR_CON_openssl_rand_hex_32

# Hosts permitidos (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1

# Entorno: development | staging | production
ENVIRONMENT=production

# =====================================================
# SSL / HTTPS
# =====================================================
# Dominio del servidor (para SSL)
DOMAIN=tu-dominio.com

# Rutas de certificados SSL (Let's Encrypt o personalizados)
SSL_CERT_PATH=/etc/nginx/ssl/server.crt
SSL_KEY_PATH=/etc/nginx/ssl/server.key

# =====================================================
# PDF Export Configuration
# =====================================================
# Ruta absoluta a wkhtmltopdf (auto-detección si vacío)
WKHTMLTOPDF_PATH=

# Fallback a HTML si wkhtmltopdf no disponible
PDF_FALLBACK_TO_HTML=true

# =====================================================
# Logging y Monitoreo
# =====================================================
# Nivel de log: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Directorio de logs
LOG_DIR=/app/logs
