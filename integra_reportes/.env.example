# Configuración de Base de Datos Local
# Duplica este archivo como .env y ajusta los valores

DB_HOST=localhost
DB_PORT=5432
DB_NAME=integra_rls
DB_USER=postgres
DB_PASSWORD=admin

# Pool de Conexiones
DB_POOL_MIN=1
DB_POOL_MAX=10

# Sesión
SESSION_TIMEOUT=60
